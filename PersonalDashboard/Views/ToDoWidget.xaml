<UserControl x:Class="PersonalDashboard.Views.ToDoWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalDashboard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Transparent">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input row -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <!-- Two-way binding keeps VM property in sync as you type -->
            <TextBox Width="240"
                     Text="{Binding NewTaskDescription, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0"/>
            <Button Content="Add" Command="{Binding AddTaskCommand}"/>
        </StackPanel>

        <!-- List of tasks -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Tasks}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="True" Margin="0,2">
                        <!-- CheckBox bound to IsCompleted -->
                        <CheckBox IsChecked="{Binding IsCompleted}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <!-- Task text -->
                        <TextBlock Text="{Binding Description}" VerticalAlignment="Center"/>
                        <!-- Remove button docks to the right -->
                        <Button Content="✕"
                                DockPanel.Dock="Right"
                                Command="{Binding DataContext.RemoveTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Margin="8,0,0,0"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>