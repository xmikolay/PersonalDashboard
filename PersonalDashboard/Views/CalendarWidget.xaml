<UserControl x:Class="PersonalDashboard.Views.CalendarWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalDashboard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Calendar: no custom header, let WPF handle month nav + month title -->
        <Calendar Grid.Row="0"
                  SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                  SelectionMode="SingleDate"
                  IsTodayHighlighted="True"
                  Margin="0,0,0,8"/>

        <!-- Events list: only shows when HasEvents = true -->
        <Border Grid.Row="1"
                Padding="8"
                BorderBrush="#33000000"
                BorderThickness="1"
                CornerRadius="6"
                Visibility="{Binding HasEvents, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <!-- Small header -->
                <TextBlock Text="Events"
                           FontWeight="SemiBold"
                           Opacity="0.8"
                           Margin="0,0,0,6"/>

                <!-- Compact list of event titles; scroll if needed -->
                <ItemsControl ItemsSource="{Binding EventsForSelectedDate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <!-- Small bullet -->
                                <Ellipse Width="6" Height="6" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <!-- Keep the panel from growing too tall -->
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="80">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
